Java version: 1.8.0_131
Java build [JVM]: 1.8.0_131-b11

execution through the command line:
	  java -jar data_set_full_path(very required!)
		     algorithm[knn or dt(knn is default,not required)]
                     training_set_size[> 0(80% is default,not required)]
                     uniform_distribution_to_data_set[true or false(false is default,not required)]
                     need_to_plot_learning_curve[true or false(false is default,not required)]
                     weighted_computation[knn neighbors(false is default,not required)]
                     k_neighbors[int > 0(7 is default,not required)]
                     distance_parameter[minkovski distance with some p > 0 - knn parameter(not required,2 is default)]

(*) no external packages! everything is standard use!
[- for more deeper information you can rean code auditing and/or my java doc into the code -]

Thanks and Have fun!
Itay Guy - 305104184